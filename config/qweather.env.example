# Copy this file to `config/qweather.env.local` and fill in the actual values.
# The `.local` file is git-ignored and should never be committed.

# Combined credential in the format credentialId|projectId|[apiHost|]base64PrivateKey
QWEATHER_API_KEY=credentialId|projectId|base64PrivateKey

# Optional custom API host (omit or leave empty to use the default api.qweather.com)
QWEATHER_API_HOST=ma4wcmc6h6.re.qweatherapi.com
