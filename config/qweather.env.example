# Copy this file to `config/qweather.env.local` and fill in the actual values.
# The `.local` file is git-ignored and should never be committed.

# Plain bearer token returned by the credential server.
# Request a fresh token regularly and keep this file out of version control.
QWEATHER_API_TOKEN=your-long-lived-or-periodically-updated-token
